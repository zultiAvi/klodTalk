Coder summary — Route hook events to per-session hook_events.jsonl
=================================================================

Implemented all steps in the plan exactly. No deviations.

Step A2 — server/session_log.py
  Added `append_hook_event(session_id, line)` between `append_raw` and
  `read_events`. Writes to `<session_log_dir>/hook_events.jsonl` (per-session,
  capital-T path). Wrapped in try/except — never raises. Uses the existing
  `session_log_dir` helper and module logger.

Step A1 — server/server.py (~line 1258)
  Replaced `session_log.log_event(session_id, "hook", line)` with
  `session_log.append_hook_event(session_id, line)`. Surrounding try/except,
  the offset bookkeeping, and the "Stop advancing on logging failure so we
  retry next poll" behaviour are unchanged.

Step B1 — clients/web/index.html
  - Added defensive guard at top of `case 'new_message':`:
        if (msg.role === 'hook') return;
  - Added defensive filter on history load so old sessions whose
    `events.jsonl` already contains `role: "hook"` rows render cleanly:
        sessionMessages[s.session_id] = (s.messages || []).filter(m => m && m.role !== 'hook');
  - Removed dead CSS rule `.log-role-badge.hook` (line 563).
  - Did NOT modify the Logs overlay code (`renderLogEvents`,
    `showAgentLogs`, ~lines 2627-2807).

Step C1 — Documentation
  - Updated server/CLAUDE.md: bullet for `session_log.py` now documents
    that hook events go to a sibling `hook_events.jsonl` via
    `append_hook_event`, NOT into `events.jsonl`/`log.txt`, and notes the
    Logs UI button is unrelated to this sink.
  - Appended new instinct bullet to .klodTalk/instincts.md describing
    the hook-event sink and warning against regression.

Pre-commit self-check
  - Grep for `log_event\([^)]*hook`  ->  0 matches.
  - Grep for `log-role-badge.hook`   ->  0 matches.
  - python3 -c "import ast; ast.parse(...)" on session_log.py and server.py: OK.
  - No TODO/FIXME/HACK/XXX/PLACEHOLDER markers introduced.
  - PostToolUse hook script untouched (still exits 0).
  - All session_log.* call sites in server.py remain wrapped in try/except.
  - Path format `/tmp/klodTalk/<id>.klodTalk/` (capital T, suffix) preserved.

Note: Existing closed sessions whose `events.jsonl` already contains
`role: "hook"` rows are left intact on disk. The new UI filter hides them
on reopen, so users see a clean replay without us mutating historical data.
